# System Diagrams: University Network Architecture

> Source: `university-network-architecture` repository  
> Generated by: Diagram Agent  
> Last updated: 2026-03-05  
> Data currency: Kantra-site live (Dec 28, 2025); Katamia/Arish templated

---

## Diagram 1: Multi-Site Network Topology

```mermaid
graph TB
    subgraph INTERNET["Internet (Public)"]
        ISP["ISP Infrastructure"]
    end

    subgraph KANTRA["Kantra-site — SU-Kantara (Main Campus)"]
        FGT_K["FortiGate-1101E\nv7.6.4 | SU-Kantara\n9 interfaces active / 34 total"]
        LAG_INSIDE["Inside LAG\nport2-5 (4 ports)"]
        LAG_DMZ["DMZ LAG\nport6-7 (2 ports)"]
        LAG_SS["Site-Site LAG\nport8-9 (2 ports)"]
        subgraph KANTRA_NETS["Internal Networks"]
            LAN_K["LAN 192.168.1.0/29\nVLAN 406"]
            VPNGW_K["VPN GW 192.168.1.8/29\nVLAN 407"]
            DMZ_SRV_K["DMZ-Server 172.17.1.0/24\nVLAN 411 | 9 published services"]
            DMZ_VOICE_K["DMZ-Voice 172.17.0.0/24\nVLAN 412"]
            MGMT_K["Management 10.1.127.0/24"]
        end
        WAN1_K["WAN-1 41.33.39.162/28\nVLAN 401 — Primary"]
        WAN2_K["WAN-2 41.33.24.66/28\nVLAN 402 — Backup"]
    end

    subgraph KATAMIA["Katamia-site — Cairo (Branch)"]
        FGT_KAT["Cisco ASA 5545-X\nKatamia Edge Firewall"]
        subgraph KATAMIA_NETS["Internal Networks"]
            LAN_KAT["LAN 10.128.0.0/16"]
            DMZ1_KAT["DMZ 172.21.0.0/24"]
            DMZ2_KAT["DMZ 172.22.0.0/24"]
            DMZ3_KAT["DMZ 172.23.0.0/24"]
            DMZ4_KAT["DMZ 172.24.0.0/24"]
            VPNGW_KAT["VPN GW 192.168.12.0/28"]
        end
        WAN_KAT["WAN 41.33.36.98\nPublic IP"]
    end

    subgraph ARISH["Arish-site (Remote Branch)"]
        FGT_A["FortiGate\nArish Edge Firewall"]
        subgraph ARISH_NETS["Internal Networks"]
            LAN_A["LAN 10.64.0.0/16"]
            DMZ1_A["DMZ 172.30.0.0/24"]
            DMZ2_A["DMZ 172.30.10.0/24"]
            DMZ3_A["DMZ 172.35.0.0/24"]
            VPNGW_A["VPN GW 192.168.14.64/28"]
        end
        WAN_A["WAN 41.33.73.218\nPublic IP"]
    end

    subgraph DC["Datacenter (Role TBD)"]
        FGT_DC["Firewall\nRole in VPN TBD"]
    end

    ISP --- WAN1_K
    ISP --- WAN2_K
    ISP --- WAN_KAT
    ISP --- WAN_A

    WAN1_K --- FGT_K
    WAN2_K --- FGT_K
    FGT_K --- LAG_INSIDE
    FGT_K --- LAG_DMZ
    FGT_K --- LAG_SS
    LAG_INSIDE --- LAN_K
    LAG_INSIDE --- VPNGW_K
    LAG_DMZ --- DMZ_SRV_K
    LAG_DMZ --- DMZ_VOICE_K

    WAN_KAT --- FGT_KAT
    FGT_KAT --- LAN_KAT
    FGT_KAT --- DMZ1_KAT

    WAN_A --- FGT_A
    FGT_A --- LAN_A
    FGT_A --- DMZ1_A

    style KANTRA fill:#1e3a5f,color:#fff
    style KATAMIA fill:#1a3a2a,color:#fff
    style ARISH fill:#3a1a1a,color:#fff
    style DC fill:#3a3a1a,color:#fff
    style INTERNET fill:#2a2a2a,color:#fff
```

---

## Diagram 2: Full-Mesh VPN Architecture

```mermaid
graph TB
    subgraph KANTRA["Kantra-site (SU-Kantara)\nFortiGate-1101E | 41.33.39.162"]
        FGT_K["FortiGate-1101E\nWAN-1: Primary\nWAN-2: Backup (SD-WAN)"]
    end

    subgraph KATAMIA["Katamia-site (Cairo)\nCisco ASA 5545-X | 41.33.36.98"]
        ASA["Cisco ASA 5545-X\nCross-vendor peer"]
    end

    subgraph ARISH["Arish-site\nFortiGate | 41.33.73.218"]
        FGT_A["FortiGate\nArish edge"]
    end

    subgraph DC["Datacenter\n(VPN role TBD)"]
        FGT_DC["Firewall\nRole under design"]
    end

    FGT_K <-->|"Cairo-S2S\nIKEv1 Aggressive | AES-128/SHA-1\n54 selectors | 10/54 active (18.5%)\n⚠️ PARTIAL — 44 selectors inactive\n~492MB RX / ~215MB TX"| ASA

    FGT_K <-->|"Arish_S2S\nIKEv2 | AES-128/SHA-256\n22 selectors | 21/22 active\n✅ UP — Full operational\n~906MB RX / ~311MB TX"| FGT_A

    ASA <-.->|"Katamia↔Arish\nTunnel status undocumented\n📋 Template — data needed"| FGT_A

    FGT_DC <-.->|"Datacenter tunnels\nRole TBD — not yet designed"| FGT_K

    subgraph REMOTE["Remote Access VPN"]
        USER["SU_Remote\nSplit-tunnel IKEv2\nPool: 10.100.90.1-100\nCurrent: 1 user (Ahmed/IT_VPN)"]
        MOBILE["Mobie_VPN\nIKEv2 | Pool: 10.100.91.101-200\n⏳ Ready, no active clients"]
        LEGACY["Remote_mobile\nIKEv1 Aggressive | DES/SHA-1\n⛔ DOWN — bound to HA interface (inactive)"]
    end

    FGT_K --- USER
    FGT_K --- MOBILE
    FGT_K -.->|"interface ha = DOWN"| LEGACY

    style KANTRA fill:#1e3a5f,color:#fff
    style KATAMIA fill:#1a3a2a,color:#fff
    style ARISH fill:#3a1a1a,color:#fff
    style DC fill:#3a3a1a,color:#fff
    style REMOTE fill:#2a1a3a,color:#fff
```

---

## Diagram 3: Security Zone Architecture (Kantra-site)

```mermaid
graph TB
    subgraph UNTRUSTED["Untrusted Zone"]
        WAN1["WAN-1\n41.33.39.162/28\nPrimary Internet"]
        WAN2["WAN-2\n41.33.24.66/28\nBackup Internet"]
    end

    subgraph FW["FortiGate-1101E Security Boundary\n67 Policies | 59 Enabled | UTM: IPS + AV + WAF + WebFilter + AppCtrl"]
        POLICY["Policy Engine\n24 user internet policies\n9 inbound DMZ NAT policies\nUser groups: IT/Managers/Faculty/Staff/Students"]
    end

    subgraph DMZ_ZONE["DMZ Zone — Published Services"]
        DMZ_SRV["DMZ-Server 172.17.1.0/24 — VLAN 411\nLMS2 | Admission | Expressway\nTelepresence | + others\nFull UTM inspection on all inbound"]
        DMZ_VOICE["DMZ-Voice 172.17.0.0/24 — VLAN 412\nVoice/UC Infrastructure\nCisco Expressway\nTelepresence"]
    end

    subgraph LAN_ZONE["Trusted LAN Zone"]
        LAN["Inside LAN\nVLAN 406: 192.168.1.0/29"]
        INTERNAL["Internal Subnets\n10.1.0.0/16 | 10.2.0.0/16\n10.3.0.0/16 | 10.5.0.0/16\n172.16.30.0/24"]
        VPNGW["VPN Gateway\n192.168.1.8/29 — VLAN 407"]
    end

    subgraph VPN_ZONE["VPN Zone — Encrypted Tunnels"]
        VPN_ARISH["Arish_S2S\nIKEv2 | ✅ Operational"]
        VPN_CAIRO["Cairo-S2S\nIKEv1 | ⚠️ Partial (10/54)"]
        VPN_REMOTE["SU_Remote\nIKEv2 EAP | ✅ Active"]
    end

    subgraph MGMT_ZONE["Out-of-Band Management"]
        MGMT["mgmt interface\n10.1.127.0/24\nHTTPS + SSH + Ping only"]
    end

    WAN1 -->|"All inbound inspected\nIPS + AV + WAF"| FW
    WAN2 -->|"SD-WAN failover path"| FW
    FW -->|"Controlled egress\nUser group policies"| LAN_ZONE
    FW -->|"Published services\nInbound NAT + UTM"| DMZ_ZONE
    FW <-->|"Encrypted tunnels"| VPN_ZONE
    MGMT_ZONE -->|"Admin access only"| FW

    style UNTRUSTED fill:#7f1d1d,color:#fff
    style FW fill:#1e3a5f,color:#fff
    style DMZ_ZONE fill:#7c3a00,color:#fff
    style LAN_ZONE fill:#14532d,color:#fff
    style VPN_ZONE fill:#1e1b4b,color:#fff
    style MGMT_ZONE fill:#4a044e,color:#fff
```

---

## Diagram 4: Datacenter Network Architecture (Target State)

```mermaid
graph TB
    subgraph DC_INTERNET["Internet Gateway"]
        DC_WAN["Datacenter WAN\nPublic IP (TBD)\nDual ISP planned"]
    end

    subgraph DC_PERIMETER["Datacenter Perimeter"]
        DC_FW["Perimeter Firewall\nModel TBD\nRole in VPN mesh: TBD"]
    end

    subgraph DC_CORE["Datacenter Core Network"]
        DC_CORE_SW["Core Switching\nHigh-speed fabric\nModel TBD"]
    end

    subgraph DC_PLATFORM["Platform Infrastructure"]
        VSPHERE["VMware vSphere\nCompute Cluster"]
        RKE2["RKE2 Kubernetes\nControl Plane Nodes (HA)"]
        STORAGE["Shared Storage\nvSAN / NFS"]
    end

    subgraph DC_SERVICES["Datacenter Services"]
        AD["Active Directory\nDomain Services\nReplication hub"]
        KEYCLOAK["Keycloak\nIdentity Broker"]
        ARGO["ArgoCD\nGitOps Reconciler"]
        FMGR["FortiManager\nCentralized FW Management"]
        FANALYZER["FortiAnalyzer\nCentralized Logging"]
    end

    subgraph BRANCH_VPN["Branch Site VPN Connections"]
        TO_KANTRA["→ Kantra-site VPN"]
        TO_KATAMIA["→ Katamia-site VPN"]
        TO_ARISH["→ Arish-site VPN"]
    end

    DC_WAN --> DC_FW
    DC_FW --> DC_CORE_SW
    DC_CORE_SW --> DC_PLATFORM
    DC_CORE_SW --> DC_SERVICES
    DC_FW <--> BRANCH_VPN

    FMGR -.->|"Policy push"| TO_KANTRA
    FMGR -.->|"Policy push"| TO_KATAMIA
    FMGR -.->|"Policy push"| TO_ARISH

    note1["⚠️ DC VPN role is TBD\nCurrent design: branches peer directly\nDC may become FortiManager/FortiAnalyzer hub only"]

    style DC_PLATFORM fill:#1e3a5f,color:#fff
    style DC_SERVICES fill:#14532d,color:#fff
    style DC_PERIMETER fill:#7f1d1d,color:#fff
    style BRANCH_VPN fill:#1e1b4b,color:#fff
```

---

## Diagram 5: VPN Encryption Standards Comparison

```mermaid
graph LR
    subgraph MODERN["Modern Standards (Recommended)"]
        IKEv2["IKEv2"]
        AES256["AES-256"]
        SHA256["SHA-256"]
        ECDH19["ECDH Group 19/20"]
        PFS20["PFS Group 20"]
    end

    subgraph ARISH_TUNNEL["Arish_S2S — ✅ Modern"]
        A_IKE["IKEv2 ✅"]
        A_ENC["AES-128 ✅"]
        A_HASH["SHA-256 ✅"]
        A_DH["DH Group 19/18 ✅"]
        A_PFS["PFS Group 20 ✅"]
    end

    subgraph CAIRO_TUNNEL["Cairo-S2S — ⚠️ Legacy (ASA Compatibility)"]
        C_IKE["IKEv1 Aggressive ⚠️"]
        C_ENC["AES-128 ✅"]
        C_HASH["SHA-1 ⚠️ Weak"]
        C_DH["DH Group TBD"]
        C_PFS["PFS Group 5 ⚠️ Weak"]
        C_NOTE["Cross-vendor: FortiGate ↔ Cisco ASA\nUpgrade path: ASA → FortiGate"]
    end

    subgraph LEGACY_TUNNEL["Remote_mobile — ⛔ Legacy (Inactive)"]
        R_IKE["IKEv1 Aggressive ⛔"]
        R_ENC["DES/3DES ⛔ Insecure"]
        R_HASH["SHA-1/MD5 ⛔ Broken"]
        R_NOTE["Bound to HA interface (DOWN)\nMust be retired or upgraded"]
    end

    style MODERN fill:#14532d,color:#fff
    style ARISH_TUNNEL fill:#1e3a5f,color:#fff
    style CAIRO_TUNNEL fill:#7c3a00,color:#fff
    style LEGACY_TUNNEL fill:#7f1d1d,color:#fff
```
